<Window x:Class="AutoRun.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoRun"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:RegEdit x:Key="reg"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource reg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel>
            <Label>Auto Run List:</Label>
        </StackPanel>
        <ListBox ItemsSource="{Binding Data}" SelectedItem="{Binding SelectedValue}" Grid.Row="1"></ListBox>
        <DockPanel Grid.Row="2" LastChildFill="False">
            <Button Command="{Binding CommandRemove}" CommandParameter="{Binding SelectedValue}" Padding="10" Margin="10" Width="100">Remove</Button>
            <Button  Command="{Binding CommandAdd}" Padding="10" Margin="10" Width="100" DockPanel.Dock="Right">Add</Button>
        </DockPanel>
    </Grid>
</Window>
