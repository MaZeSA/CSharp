<UserControl x:Class="Battleship.ViewModel.GamePanels.Pixels.EntyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Battleship.ViewModel.GamePanels.Pixels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="#FFA0A0A0" BorderBrush="Black" BorderThickness="1" AllowDrop="True">
            <StackPanel Orientation="Horizontal">
                 <TextBlock Text="{Binding Row}"/>
                <TextBlock Text="-"/>
               <TextBlock Text="{Binding Column}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
